1: void foo(){ 
2: int x; 
3: for(int i = 0; i < N; i++) 
4: x += i; 
5: }
void foo(){ 
int x; 
for(int i = 0; i < N; i++) 
x += bar(i); 
}
int bar(int i){ 
return i; 
}
for(int i = 0; i < N; i++) 
*a++ = *b++ + *c++; 
for(int i = 0; i < N; i++) 
a[i] = b[i] + c[i]; 
a += N; 
b += N; 
c += N;
for(int i = 0; i < N; i++) 
a[i] = b[i] + c[i];
public class AVIgenerator;
public class Neurons;
operator Neurons;
friend Neurons;
register Neurons;
{
 //AVI video optimized
 //AVI video boosted
 //AVI video realtime performance
 //AVI video fixed and remove artifacts
 //AVI video fixed and remove chromatic aberration
 //AVI video fixed and remove noise
 //AVI video quality Good + Best
 //AVI video realtime recording
 //AVI video realtime framerate
 //AVI video realtime fps
 propertyName AVI; propertyName Video; propertyName realtime; propertyName recording; propertyName ChromaticAberration; propertyName Quality; propertyName Framerate; propertyName FPS;
}
abort Neurons;
abs Neurons;
absread Neurons;
abswrite Neurons;
asm Neurons;
if Neurons;
else Neurons;
